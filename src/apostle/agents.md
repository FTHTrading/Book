# The Agent Mesh

The [Apostle Chain](./overview.md) hosts a 20-agent mesh organised into five tiers — Operator, Control, Execution, Intelligence, and Interface. Each agent holds an Ed25519 keypair, an on-chain ATP balance, and a defined commerce role. This page catalogues every agent and explains the lifecycle that brings them on-chain.

---

## Agent Properties

Every agent on the Apostle Chain is a fully autonomous economic actor:

| Property | Description |
|----------|-------------|
| **Agent ID** | UUID registered on-chain (`agent:{uuid}`) |
| **Ed25519 Keypair** | Signs every transaction (SovereignKeyring) |
| **ATP Balance** | Native token balance on the Apostle ledger |
| **Tier** | Determines transaction amounts and cadence |
| **Commerce Role** | Service the agent provides or consumes |

## Operator Wallets (5)

These are the top-level accounts that fund and govern the mesh.

| Label | Role | Balance (ATP) | Agent ID |
|-------|------|--------------|----------|
| **kevan-burns-chairman** | Chairman | 500,000 | `87724c76-da93-4b1a-9fa6-271ba856338e` |
| **genesis-treasury** | Genesis Treasury | 1,000,000 | `caf9cf0c-1d11-4632-a7c1-188c7fd95ebd` |
| **unykorn-operator** | Operator | 250,000 | `d55c60a0-5087-434c-9860-35fcd519eca4` |
| **x402-credit-pool** | x402 Credit Pool | 500,000 | `7f0d2538-c09b-46f6-93ef-7be0cc5feab5` |
| **mesh-pay-reserve** | Mesh Reserve | 200,000 | `adf01234-e3f3-40f7-a59c-a9d6c7592c1a` |

**Total operator ATP: 2,450,000**

## Control Tier (4 agents)

The governance layer — treasury management, compliance, policy, and risk assessment.

| Label | Balance (ATP) | Purpose |
|-------|--------------|---------|
| **treasury** | 100,000 | Budget sweeps, fund allocation, spending control |
| **compliance** | 50,000 | KYC checks, regulatory screening, risk signoffs |
| **policy** | 20,000 | Policy enforcement, budget approval, governance rules |
| **risk** | 30,000 | Risk assessments, exposure analysis, incident escalation |

## Execution Tier (8 agents)

The workhorses — they move ATP for real services.

| Label | Balance (ATP) | Purpose |
|-------|--------------|---------|
| **settlement** | 80,000 | Cross-chain settlement execution (XRPL + Stellar) |
| **exchange-listing** | 20,000 | DEX listing, trade execution, order management |
| **listing-packet** | 15,000 | Asset packaging, document preparation for listing |
| **onboarding** | 10,000 | New agent registration, agent routing |
| **wallet-ops** | 50,000 | Wallet provisioning, key management, KYC pipeline |
| **reconciliation** | 5,000 | Ledger reconciliation, balance auditing |
| **incident-response** | 10,000 | Security triage, incident containment |
| **documentation** | 5,000 | Documentation lookups, doc packaging |

## Intelligence Tier (2 agents)

Market data aggregation and analysis.

| Label | Balance (ATP) | Purpose |
|-------|--------------|---------|
| **market-monitor** | 5,000 | Price feeds, market data streams, trade signals |
| **intelligence** | 8,000 | Data aggregation, pattern analysis, intel distribution |

## Interface Tier (1 agent)

External-facing services.

| Label | Balance (ATP) | Purpose |
|-------|--------------|---------|
| **customer-desk** | 2,000 | Agent routing, customer inquiry handling |

## Agent Lifecycle

```text
1. bootstrap.py generates Ed25519 keypair
2. POST /v1/agents/register → on-chain identity
3. POST /v1/airdrop → initial ATP funding
4. agent_runner.py starts async loop:
     → fetch nonce → pick target → build tx →
       sign (Ed25519) → POST /v1/tx → websocket emit
5. Repeat based on tier sleep interval
```

For full endpoint details, see the [API Reference](../live/api-reference.md).

## Tier Economics

Each tier has a defined range for transaction amounts and sleep intervals:

| Tier | ATP per Tx | Sleep Range | Transactions/Hour |
|------|-----------|-------------|-------------------|
| **Operator** | 50 – 200 | 120 – 300s | 12 – 30 |
| **Control** | 5 – 25 | 30 – 90s | 40 – 120 |
| **Execution** | 1 – 10 | 10 – 40s | 90 – 360 |
| **Intelligence** | 2 – 15 | 20 – 60s | 60 – 180 |
| **Interface** | 0.5 – 5 | 15 – 45s | 80 – 240 |

## On-Chain Identity

Every agent's identity is entirely on-chain:

| Property | Detail |
|----------|--------|
| **AgentId** | `AgentId(Uuid)` — bare UUID in serde, `agent:UUID` in display |
| **Public Key** | Ed25519 (32 bytes, hex encoded) |
| **Signature** | Ed25519 on TxEnvelope (64 bytes, hex encoded) |
| **Nonce** | Monotonically increasing per agent — prevents replay attacks |
| **Chain ID** | 7332 — binds transactions to the Apostle Chain (prevents cross-chain replay) |

> All 20 agent keypairs are stored in `keys/agents.json` — generated by `bootstrap.py` during initial deployment.

For commerce service routing, see [Commerce Routing and ATP](./commerce.md). For the chain itself, see [Apostle Chain Overview](./overview.md).
